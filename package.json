{
  "private": true,
  "version": "0.0.0",
  "name": "example",
  "devDependencies": {
    "jest": "^29.0.0"
  },
  "scripts": {
    "test": "jest",
    "start": "json-server ./db/db.json"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
